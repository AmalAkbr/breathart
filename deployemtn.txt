2️⃣ Create a dedicated user (optional but recommended)

It’s best practice not to run Node as root. You can create a breathart user:

sudo adduser breathart
sudo usermod -aG sudo breathart

Move your project folder to the new user home (optional):


sudo chown -R breathart:breathart /home/muhammadhabeebrahmam/web/breathart
mv /home/muhammadhabeebrahmam/web/breathart /home/breathart/breathart


Switch to the new user:
su - breathart
cd ~/breathart

Running Node as a normal user prevents accidental system-wide damage.



Option 2 — Move project to the user who will run the app
Create a dedicated breathart user if you want
Move the project files there:
sudo mkdir -p /home/breathart/web
sudo cp -r /home/muhammadhabeebrahmam/web/breathart /home/breathart/web/
sudo chown -R breathart:breathart /home/breathart/web/breathart
Log in as breathart, then install Node & npm for that user
cd /home/breathart/web/breathart/backend → npm ci
cd ../frontend → npm ci && npm run build

check users : ls/home
goto user sudo su - username


sudo chown -R breathart:breathart ~/web
Explanation:

sudo → you need root permissions to change ownership of files that belong to root.
chown → change owner.
-R → recursively, applies to all files and subfolders inside ~/web.
breathart:breathart → set user and group to breathart.
~/web → path to your web directory.






+=====================================

Ah — I see exactly what’s going on. The warnings:

conflicting server name "habeeb.qzz.io" on 0.0.0.0:80, ignored
conflicting server name "habeeb.qzz.io" on 0.0.0.0:443, ignored

mean that Nginx already has another server block for habeeb.qzz.io (probably in /etc/nginx/sites-enabled/default). So now you have two server blocks claiming the same domain on the same ports. Nginx ignores one of them, which can cause unexpected behavior.

✅ How to fix it
Check what’s enabled:
ls -l /etc/nginx/sites-enabled/

You will probably see both default and habeeb.qzz.io linked.

Remove the default one (or any old conflicting blocks):
sudo rm /etc/nginx/sites-enabled/default
Make sure only your cleaned config exists:
sudo nginx -t
sudo systemctl reload nginx

After this, Nginx should no longer show the conflicting server name warnings, and https://habeeb.qzz.io
 should properly route to your Node.js app on port 8080.


 ===================


 sudo npm install -g pm2


 pm2 start npm --name "breathart" -- run prod:backend

 Explanation:

npm → tells PM2 to run npm scripts.
--name "breathart" → gives the process a friendly name for PM2.
-- run prod:backend → runs your prod:backend script.
======================================================

3️⃣ Check that it’s running
pm2 list
pm2 logs breathart
pm2 list shows all PM2 processes.
pm2 logs breathart streams logs from your app.
4️⃣ Make it auto-start on reboot
pm2 save
pm2 startup
pm2 save remembers your processes.
pm2 startup generates a system service so Node restarts if the VPS reboots.

✅ Result:

Node app runs on port 8080 via npm run prod:backend.
PM2 keeps it alive and restarts on crash or reboot.
Nginx will forward HTTPS traffic to your Node app automatically.


=======================================

Nginx will still handle HTTPS properly.
Restart your app with PM2:
pm2 restart breathart
Or stop & start if needed:
pm2 stop breathart
pm2 delete breathart
============================
pm2 start npm --name "breathart" -- run prod:backend
⚡ Result

==============================================================

1️⃣ List running PM2 processes
pm2 list

Output looks like:

┌───────┬───────────┬──────┬───────┬──────────┐
│ id    │ name      │ mode │ pid   │ status   │
├───────┼───────────┼──────┼───────┼──────────┤
│ 0     │ breathart │ fork │ 12345 │ online   │
└───────┴───────────┴──────┴───────┴──────────┘

Take note of either the id or the name of the process.

2️⃣ Stop a running process
By name:
pm2 stop breathart
Or by id (e.g., 0):
pm2 stop 0

This stops the process but keeps it in PM2’s list (you can restart it later).

3️⃣ Delete a process completely
By name:
pm2 delete breathart
Or by id:
pm2 delete 0

This removes it from PM2, freeing it from auto-start.

4️⃣ Optional: Stop all PM2 processes
pm2 stop all

Or delete all:

pm2 delete all
5️⃣ Clean saved startup list (if auto-start is set)
pm2 save
After deleting or stopping processes, run pm2 save to update the auto-start list.

💡 Summary:

stop → stops process but keeps it in PM2 list.
delete → completely removes it.
Always check with pm2 list to confirm.

===============================





Useful PM2 commands:
Stop a process:
pm2 stop breathart   # by name
pm2 stop 0           # by PM2 id
Delete a process (remove completely from PM2 list):
pm2 delete breathart
pm2 delete 0
Restart a process:
pm2 restart breathart
Show detailed info:
pm2 show breathart


